import LoginBackground from "./components/LoginBackground"
import LoginPanel from "./components/LoginPanel"

export default function Login() {

    return (
        <main className="w-full h-[92vh] overflow-hidden snap-mandatory">
            <LoginPanel/>
        </main>
    )
}